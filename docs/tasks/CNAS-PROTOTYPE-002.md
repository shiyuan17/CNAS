# CNAS-PROTOTYPE-002 CNAS LIMS 候选仿真原型

- 工作流档位：轻量
- 当前阶段：交付
- 当前状态：空闲
- 处理结果：完成

## 目标

在不改变候选 SRS、`BL-BIZ-1.0` 和 `BL-DEV-1.0` 未批准状态的前提下，交付一个独立的、可本地运行的 26 页 CNAS LIMS 业务走查原型。原型必须使用虚构模拟数据和浏览器 `localStorage`，覆盖检测、校准及共享质量闭环的关键正常与异常交互。

## 验收标准

| AC-ID | 标准 |
| --- | --- |
| AC-01 | `prototype-spec.md` 与 `prototype-manifest.json` 明确候选定位、边界、26 页范围、角色、需求引用、状态和待确认项。 |
| AC-02 | 原型提供工作台、待办、检测与校准分支、记录复核、报告/证书、资源、质量、认可和平台控制等 26 个唯一逻辑页面。 |
| AC-03 | 所有交互仅写入本地模拟状态；角色切换、提交、退回、阻断、发布失败恢复、CAPA 关闭、权限拒绝、审计和重置可见且可恢复。 |
| AC-04 | 页面持续显示“候选原型，未批准”和与页面相关的待业务确认说明；不接入真实登录、签名、接口或生产数据。 |
| AC-05 | 静态校验、治理校验、差异空白检查以及桌面/窄屏浏览器关键路径验证均通过。 |
| AC-06 | `TBD` 工作簿的 P0 门禁明确为 24 项，包含 `TBD-024`，并将 `TBD-023` 后置至迁移范围确认后的技术基线。 |

## 验证计划

- 运行 `node scripts/validate-cnas-prototype.mjs`，核对页面、清单、运行时、候选标识和 P0 TBD 门禁。
- 通过本地 HTTP 服务以 `1440px` 和 `390px` 视口验证工作台下钻、检测/校准分支、退回、资源阻断、发布失败恢复、CAPA、权限拒绝、审计和重置。
- 运行 `node .agents/cognis/governance/validate.mjs` 与 `git diff --check`。
- 本次由需求方明确要求不执行独立 Tester 与 Reviewer；保留静态、治理和浏览器验证作为构建证据，不将其表述为独立审查结论。

## 下一步动作

原型已完成构建和本轮验证。页面继续作为候选走查材料，不得被标记为正式基线或作为 E1 业务事实。

## 执行约束

- 本次由需求方明确要求直接构建，不执行独立 Tester 与 Reviewer。
- 写入范围限定为 `html_design/`、`scripts/validate-cnas-prototype.mjs`、P0 TBD 门禁工作簿及本任务记录；不恢复历史原型资产。
- 仅使用浏览器本地模拟状态和脱敏虚构数据，不连接真实账户、电子签名、仪器、物联网、消息或外部接口。

## 验收证据

| AC-ID | 证据类型 | 命令或产物 | 退出码 | 核验时间 | 核验者 | 实际结果 |
| --- | --- | --- | --- | --- | --- | --- |
| AC-01 | 命令 | `node scripts/validate-cnas-prototype.mjs` | 0 | 2026-07-28 | 实现负责人 | 26 页清单、候选标识与运行时通过。 |
| AC-02 | 命令 | `node scripts/validate-cnas-prototype.mjs` | 0 | 2026-07-28 | 实现负责人 | 26 个唯一页面、模块、需求引用与导航关系通过。 |
| AC-03 | 人工 | 项目受管 Playwright，`http://127.0.0.1:4183/index.html` | 0 | 2026-07-28 | 实现负责人 | 工作台下钻、检测/校准分支、资源阻断、发布失败恢复、CAPA、权限拒绝、审计与重置通过。 |
| AC-04 | 人工 | 项目受管 Playwright，`http://127.0.0.1:4183/index.html` | 0 | 2026-07-28 | 实现负责人 | 页面展示候选标识与待确认说明；新会话控制台 0 error/0 warning，本地清单请求 200。 |
| AC-05 | 命令 | `node .agents/cognis/governance/validate.mjs` | 0 | 2026-07-28 | 实现负责人 | 治理校验通过。 |
| AC-05 | 命令 | `git diff --check` | 0 | 2026-07-28 | 实现负责人 | 未发现空白错误；仅输出既有文件的 CRLF 转换警告。 |
| AC-05 | 人工 | 项目受管 Playwright 截图 | 0 | 2026-07-28 | 实现负责人 | 1440px 与 390px 截图已检查，无页面重叠；窄屏侧栏折叠、表格横向滚动。 |
| AC-06 | 命令 | `node scripts/validate-cnas-prototype.mjs` | 0 | 2026-07-28 | 实现负责人 | P0 TBD 门禁为 24 项，包含 `TBD-024`，`TBD-023` 后置。 |

## 剩余风险

- E1 业务证据、25 项 `TBD-*` 决策和四方签署均未完成，原型不得作为正式需求或开发依据。
- 本地模拟不能证明真实电子签名、数据完整性、接口补偿、并发、性能、安全、备份恢复或浏览器兼容要求。
- 本次未执行独立 Tester 与 Reviewer，构建证据不构成独立核验或红队审查结论。
