# CNAS LIMS MVP 候选仿真原型

打开 `index.html`，或在本目录启动任意静态 HTTP 服务后访问它。该原型只使用虚构示例和浏览器 `localStorage`；“重置模拟”会清除本原型的本地状态。

视觉 token、导航骨架、表单、表格、流程反馈、抽屉和状态语义以 `../design/cnas-design-system.png` 为参考。它不是业务或开发基线，也不连接真实账户、电子签名、仪器或外部接口。
